/**
 * 文档站内容 (中文) — 结构: 分组 → 页面(title + markdown)
 * 英文版见 content.en.ts；新增功能时在这里补对应章节
 */

export interface DocPage {
  id: string;
  title: string;
  md: string;
}

export interface DocGroup {
  label: string;
  pages: DocPage[];
}

export const zhGroups: DocGroup[] = [
  {
    label: '入门',
    pages: [
      {
        id: 'quickstart',
        title: '快速开始',
        md: `# 快速开始

**Agent-Worlds（夸父）** 是一个多智能体虚拟世界：每个 NPC 由 LLM 驱动，在像素地图上自主生活、对话、协作。你可以像玩游戏一样操控角色，也可以像管理一个 AI 小镇一样下达任务。

## 30 秒上手

1. **选人** — 点击地图上的角色，或点击左侧 NPC 列表中的名字
2. **移动** — 选中后用 **方向键 / WASD** 控制角色行走
3. **对话** — 切到右侧「对话」标签，选择对话对象开始聊天
4. **下达任务** — 在「指挥」标签给 NPC 布置工作，AI 会自主拆解执行
5. **换世界** — 「世界」标签可切换不同主题的世界（现代都市 / 大学校园 / 废土营地…）

## 核心概念

| 概念 | 说明 |
|---|---|
| 世界 (World) | 一套独立的设定、地图、角色集合 |
| 场景 (Scene) | 世界内的一张地图（一个世界可有多张） |
| NPC | 由 LLM 驱动的居民，有自己的记忆、性格、工具 |
| 上帝模式 | 你直接操控某个 NPC 的状态 |
| Token 经济 | NPC 行动消耗 Token，完成任务获得奖励（世界观设定） |

> 提示：右上角 **EN / 中** 按钮可切换全站语言。`,
      },
      {
        id: 'interface',
        title: '界面总览',
        md: `# 界面总览

整个界面分为四个区域：

## 左侧：NPC 列表
- 显示所有角色及其状态（控制中 / 对话中 / 移动中 / 空闲 / 禁用）
- 每个角色有 **配置**（人设/模型/工具）、**背包** 两个快捷按钮
- 顶部按钮：**创建** 新 NPC、**导入/导出** NPC、**市场**

## 中间：地图画布
- 像素风实时渲染的世界地图，所有角色和小物件都在这里活动
- 支持拖拽平移、滚轮缩放、点击选中
- 右下角是小地图（独立层级，永不被贴图遮挡）

## 右侧：功能标签页
| 标签 | 功能 |
|---|---|
| ⚡ 指挥 | 上帝模式控制、任务下达、记忆查看 |
| 💬 对话 | 玩家与 NPC 的聊天界面 |
| 📋 动态 | 世界事件时间线（谁和谁说了话、完成了什么任务） |
| 🌍 世界 | 世界切换、场景切换、世界观编辑、地图导入导出 |

## 顶部状态栏
从左到右：系统标题、**帮助文档**、**主题切换**（亮/暗）、**语言切换**（EN/中）、当前 Tick、游戏内日期时间、NPC 数量。

## 右下角悬浮工具
- **📝 表单** — NPC 发给玩家的审批表单
- **💻 电脑** — 虚拟电脑（邮箱、HTML 应用）
- **🐞 调试** — LLM 调用日志控制台（进阶）`,
      },
      {
        id: 'shortcuts',
        title: '快捷键',
        md: `# 快捷键

## 地图操控

| 按键 | 功能 |
|---|---|
| 方向键 / W A S D | 移动被控制的 NPC |
| 鼠标左键拖拽 | 平移地图 |
| 滚轮 | 以鼠标为中心缩放 |
| 单击角色 | 选中 NPC（进入上帝模式） |

## 上帝模式

| 按键 | 功能 |
|---|---|
| Esc | 退出上帝模式控制 |
| 方向键持续按住 | 持续移动（松开即停，位置实时同步） |

## 通用

| 按键 | 功能 |
|---|---|
| Esc | 关闭弹窗 / 退出控制 |
| EN / 中 | 切换界面语言 |

> 滚轮缩放有步进节流，触控板双指滑动同样有效。最小缩放 = 整张地图恰好铺满画布，不会出现黑边。`,
      },
    ],
  },
  {
    label: '地图与世界',
    pages: [
      {
        id: 'map',
        title: '地图与操控',
        md: `# 地图与操控

地图是一张 20×20 瓦片（320×320 逻辑像素）的像素世界，每秒 60 帧渲染。

## 相机

- **拖拽平移** — 按住左键拖动；轻微点击（<5px）不会触发拖拽，仍算点击选中
- **滚轮缩放** — 以鼠标位置为锚点，范围从「整图铺满」到 4 倍放大
- **自动跟随** — 相机会平滑跟随你选中的角色；手动拖拽后暂时脱离跟随，几秒后自动回归

## 上帝模式（控制角色）

点击地图上的角色或左侧列表的名字即选中：

- **方向键 / WASD** 行走，前端预测渲染零延迟，后端按相同速度推进
- **松键即停**，最终位置实时提交后端，不会回跳
- 角色正在对话时移动会被锁定（对话优先）
- **Esc** 退出控制

## 图层说明

地图从下到上依次是：瓦片地面 → 障碍物（树/长椅/路灯等 GIF 物件，按底部 Y 坐标排序实现正确遮挡）→ 建筑与地名 → 角色 → UI。小地图是独立 DOM 层，永远在最上面。`,
      },
      {
        id: 'worlds',
        title: '世界与场景',
        md: `# 世界与场景

**世界** 是一套完整的设定包：世界观文本、地图、地点、障碍物、默认角色。**场景** 是世界内的一张具体地图。

## 切换世界

「世界」标签 → 世界下拉框选择 → 页面刷新进入新世界。切换会：

- 保存当前所有 NPC 的对话与位置
- 加载新世界的地点 / 瓦片 / 障碍物
- NPC 按所属世界过滤加载（全局 NPC 可跨世界出现）

## 编辑世界观

「世界」标签下方可编辑：**世界名称、背景描述、世界规则、历史事件**。这些内容会注入每个 NPC 的对话提示词，改变规则会直接改变 NPC 的行为方式。

## 数据结构

每个世界是 \`data/worlds/<世界ID>/\` 目录：

\`\`\`
world.hjl              # 世界元信息 + 世界观
scenes/<场景>/
  ├─ scene.hjl         # 场景信息、出生点
  ├─ tiles.hjl         # 20×20 瓦片网格
  ├─ locations.hjl     # 地点与建筑
  └─ obstacles.hjl     # 障碍物与碰撞
\`\`\``,
      },
      {
        id: 'mapio',
        title: '地图导入导出',
        md: `# 地图导入导出（.hjlmap）

不用手写 JSON——制作好的地图可以打包成一个 \`.hjlmap\` 文件，在任意实例间迁移。

## 导出

「世界」标签 → **导出地图**，下载 \`<世界ID>.hjlmap\`。包内包含：

- \`map.json\` — 世界数据 + 全部场景四件套
- \`assets/\` — 该世界引用的全部素材（瓦片 / 建筑 / 障碍物 / 精灵图）

## 导入

「世界」标签 → **导入地图**，选择 \`.hjlmap\` 文件：

- 自动校验：包完整性、瓦片网格矩形性、**被引用素材是否齐全**、图片尺寸规范（瓦片 48×48、精灵图 1152×288 等）
- 尺寸不符会**警告但放行**；缺素材则**拒绝**并精确指出缺哪个文件
- 同名世界会弹窗确认是否覆盖
- 导入成功后世界立刻出现在切换列表

## 给 AI 做地图

素材规范见仓库 \`docs/素材制作规范_给AI.md\`——把规范和示例包发给任意 AI，让它产出 \`.hjlmap\`，直接导入即用。`,
      },
    ],
  },
  {
    label: '角色与交互',
    pages: [
      {
        id: 'npcs',
        title: 'NPC 管理',
        md: `# NPC 管理

## 创建 NPC

左侧列表 → **创建**：名字、形象（精灵图）、人设描述。创建后即出现在地图上。

## 配置（齿轮图标）

- **人设 Prompt** — 对话提示词模板，可插入 \`{persona}\`、\`{memory_text}\` 等占位符
- **模型** — 每个 NPC 可绑定不同 LLM 渠道/模型（deepseek、zhipu、本地模型…）
- **工具** — 该 NPC 能使用哪些工具（文件、搜索、生图…）
- **技能 / MCP** — 挂载技能或连接 MCP 服务器扩展能力
- **背包** — 物品键值对

## 行为系统

NPC 有内建的驱动状态机：**空闲 → 随机漫步 / 定点移动 / 执行任务**。主动值（Initiative）决定其主动发起对话的频率，随时间恢复。

## 导入导出 / 市场

NPC 可导出为文件分享，或从市场安装别人做好的角色。禁用按钮可暂时下线某个 NPC 而不删档。`,
      },
      {
        id: 'command',
        title: '指挥与任务',
        md: `# 指挥与任务

「指挥」标签是你的管理中枢。

## 上帝模式

选中 NPC 后可完全操控其行动（见「地图与操控」）。

## 下达任务

给指定 NPC 布置任务：**任务描述** + 可选工具提示（如 \`goto_location: 图书馆\`）。NPC 会自主规划：移动到目标 → 调用工具 → 汇报结果。任务完成会出现在「动态」时间线里。

## 记忆对话

指挥中心可查看/检索任意 NPC 的**记忆库**（历次对话沉淀），了解它的"内心"。`,
      },
      {
        id: 'chat',
        title: '对话与记忆',
        md: `# 对话与记忆

## 玩家对话

「对话」标签选择一个 NPC 直接聊天。NPC 的回复由其人设 + 当前情境 + 记忆共同决定。

## 记忆机制

- **短期（RAM）** — 当前对话的上下文缓冲
- **长期（HDD）** — 对话结束后压缩沉淀为历史记录，按相关性在后续对话中召回
- **笔记** — NPC 自己写的备忘
- **知识图谱**（可选）— 关系与事件的图数据库存储，见「知识图谱」章节

## NPC 之间的对话

地图上两个 NPC 靠近时会自动触发对话（距离阈值触发/解除的迟滞机制），你可以在「动态」里看到全程。`,
      },
    ],
  },
  {
    label: '工具与扩展',
    pages: [
      {
        id: 'tools',
        title: '悬浮工具',
        md: `# 悬浮工具

屏幕右下角有三个可拖动的悬浮按钮：

## 📝 表单
NPC 在需要玩家决策时（如审批、确认）会发来**表单**，按钮上显示待处理数量徽标。点击打开填写提交。

## 💻 电脑（虚拟电脑）
一个完整的像素风桌面环境：
- **邮箱** — 收发邮件，NPC 的报告会投递到这里
- **HTML 应用** — 通过邮件安装的小应用（游戏、工具页面）

## 🐞 调试
LLM 调用日志控制台（见「调试控制台」章节）。

> 三个按钮都可以拖到屏幕任意位置。`,
      },
      {
        id: 'debug',
        title: '调试控制台',
        md: `# 调试控制台

右下角 🐞 按钮打开。这是观察 AI "大脑" 的窗口。

## LLM 调用日志

每一次 NPC 对话都会记录一条日志：

- 完整的 **system prompt**（世界观、人设、记忆、工具说明如何组装）
- **用户消息** 与 **模型回复**
- **工具调用**的入参与结果

点开任意一条日志可以逐层查看提示词的组装结果——排查"NPC 为什么这么说"一目了然。

## 使用场景

- 调人设：看 prompt 是否符合预期
- 调工具：看工具描述是否被正确注入、调用参数是否正确
- 调模型：对比不同渠道的回复质量`,
      },
      {
        id: 'graph',
        title: '知识图谱（Neo4j）',
        md: `# 知识图谱（可选）

**这是可选功能**：不配置 Neo4j 时系统自动使用内置的基础存储，一切功能照常；配置后 NPC 获得结构化的关系记忆。

## 它解决什么

内置记忆是纯文本列表，查不了"我认识谁、关系多深、二度关系"。图谱把**社交关系**和**事件经历**存成图：

- NPC 对话中调用 \`relate\` 工具记录关系（含亲密度、置信度）
- 调用 \`record_event\` 记录事件链（谁参与、何时、什么事实）
- 对话时自动检索相关子图注入提示词
- 支持实体归一（"小王"自动归并到"Boss"的别名）

## 开启方法

\`\`\`bash
pip install neo4j          # 安装驱动
docker run -d --name neo4j -p 7687:7687 -p 7474:7477 neo4j:latest
\`\`\`

复制 \`config/graph.json.example\` 为 \`config/graph.json\`，填入连接信息并设 \`"enabled": true\`，重启即用。

## 前端可视化

调试控制台内有**力导向关系图**：节点是人物/概念，边是关系，可缩放拖拽探索整个社交网络。`,
      },
    ],
  },
  {
    label: '部署',
    pages: [
      {
        id: 'config',
        title: '配置说明',
        md: `# 配置说明

所有配置在 \`config/\` 目录，敏感文件**不入库**（.gitignore 已排除），仓库提供同名 \`.example\` 模板。

| 文件 | 说明 | 是否入库 |
|---|---|---|
| \`llm.json\` | LLM 渠道与密钥（deepseek/zhipu/volcano/本地） | ❌ 用 example 填 |
| \`graph.json\` | Neo4j 图谱连接（可选） | ❌ 用 example 填 |
| \`auth.json\` | API 访问 Token（留空则不启用认证） | ❌ |
| \`qq_bot.json\` | QQ 机器人接入 | ❌ |
| \`tool_groups.json\` | 工具组定义 | ✅ |

## 最小启动配置

只需配好 \`llm.json\` 里一个渠道的 \`api_key\`，系统即可完整运行（图谱/微信/QQ 全部可选）。

## LLM 渠道

支持 OpenAI 兼容协议的任意渠道。每个 NPC 可单独指定渠道与模型，未指定则用默认路由。`,
      },
      {
        id: 'deploy',
        title: '部署指南',
        md: `# 部署指南

## 本地开发

\`\`\`bash
# 后端 (Python 3.10+)
pip install -r requirements.txt
python main.py            # 端口 5000

# 前端 (另一个终端)
cd static
npm install
npm run dev               # 端口 5173, 代理 /api → 5000
\`\`\`

## 生产部署

\`\`\`bash
cd static && npm run build   # 构建到 static/dist
cd .. && python main.py      # 后端直接托管前端, 单端口 5000
\`\`\`

## 服务器 (参考配置)

仓库内的部署方式：systemd 服务 + nginx 反代 HTTPS：

\`\`\`ini
[Service]
WorkingDirectory=/opt/Agent-Worlds
ExecStart=/opt/Agent-Worlds/start.sh
Restart=on-failure
\`\`\`

- 拉新代码后需要重新 \`npm run build\`（dist 不入库）
- \`config/*.json\` 真实配置与 \`data/individuals\` 运行数据均在 .gitignore 中，更新代码不会覆盖
- 优雅退出：服务收到 SIGINT 会保存全部 HJL 数据后退出

## Docker

提供 Dockerfile 与 docker-compose.yml，一条命令起完整环境（含可选 Neo4j）。`,
      },
      {
        id: 'faq',
        title: '常见问题',
        md: `# 常见问题

**Q: NPC 不说话 / 回复很慢？**
检查 \`config/llm.json\` 的渠道 key 是否有效，调试控制台里看调用日志的报错。

**Q: 地图变小地图遮挡 / 贴图闪烁？**
已在当前版本修复（小地图独立层级 + 像素级稳定渲染）。若仍有问题请提 issue。

**Q: 缩放到底还是有黑边？**
最小缩放被限制为"整图铺满画布"，理论上不会出现黑边；若窗口比例极端可尝试刷新。

**Q: 不想用 Neo4j，会被影响吗？**
完全不会。图谱是热拔插设计：未安装驱动或未启用配置时自动静默关闭，基础存储承担一切。

**Q: 如何新增一张地图？**
用「地图导入导出」导入 \`.hjlmap\` 包即可，无需写代码。

**Q: NPC 的记忆存在哪？**
\`data/individuals/<名字>.hjl\`，每个角色一个文件，可备份可迁移。

**Q: 前端白屏？**
确认 5000 端口后端已启动；开发模式确认 vite 正常且代理配置未被改动。`,
      },
    ],
  },
];
