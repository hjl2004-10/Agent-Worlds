# Agent-Worlds 架构图（Mermaid）

系统核心架构图 / 流程图的 Mermaid 源码。贴入 mermaid.live、Typora、Obsidian 或 GitHub 渲染，也可导出 PNG/SVG 用于文档。

架构图用代码画（Mermaid / PlantUML）是业界标准做法：文字精确、结构清晰、可进 git 版本控制。不用 AI 文生图——其对技术框图的文字与连线渲染不可靠。

符号约定：矩形=模块/过程，菱形=判断，圆角=起止，圆柱=数据存储，实线箭头=主流向，虚线=辅助/事件。

---

## 1. 总架构

```mermaid
flowchart TB
  subgraph FE["Frontend 前端 (static)"]
    FE1[地图 / 上帝控制]
    FE2[NPC 配置 / 调试面板]
    FE3[邮箱 / 表单 / 任务 / 市场]
  end

  subgraph GW["Gateway 网关 · 90+ 端点"]
    AUTH[auth 认证 / CORS]
    ROUTER[15 路由模块]
  end

  subgraph CH["Channel 渠道"]
    LLMOUT["LLM 出站<br/>zhipu / volcano / local / claude"]
    LLMIN["对话入站<br/>wechat / qq_bot"]
  end

  subgraph SC["Scaffolding 脚手架"]
    LOAD[loader_l1 装配]
    ASM[prompt 组装]
    SK[Skill 系统 · 10 个]
  end

  subgraph HS["Harness 驾驭层"]
    SOC[social 对话循环]
    DRV[drive 移动]
    MEM[mem 记忆]
    GRP[graph 图谱]
    ANA[analyzer 分析]
  end

  subgraph TOOLS["工具生态 · 13 组 ~40 工具"]
    T1[file / task / memory / notify]
    T2[inventory / location / browser]
    T3[npc 协作 / knowledge / creative]
    T4[voice / video / graph]
  end

  subgraph DATA["数据"]
    HJL["HJL 四级分形"]
    NEO[("Neo4j 图谱")]
    WORLDS["4 worlds"]
  end

  BUS[State Bus 状态总线]

  FE --> GW
  GW --> HS
  CH -.-> HS
  SC --> HS
  HS --> TOOLS
  HS --> DATA
  BUS -.-> FE
  BUS -.-> GW
  BUS -.-> HS
  BUS -.-> SC
```

---

## 2. 工具与能力生态

```mermaid
flowchart LR
  subgraph SRC["能力来源三层"]
    MANUAL["手动工具组<br/>config/tool_groups.json · 13 组"]
    SKILL["Skill 系统<br/>工具 + 提示词 + MCP · 10 个"]
    MCPX["MCP 外部协议<br/>+ marketplace 市场"]
  end

  MERGE(("NPC<br/>rom_tools<br/>合并"))
  MANUAL --> MERGE
  SKILL --> MERGE
  MCPX -.懒加载.-> MERGE

  subgraph G["13 工具组"]
    G1["@file @task @memory @notify"]
    G2["@inventory(6) @location @browser(6)"]
    G3["@npc @knowledge @creative"]
    G4["@voice @video @graph"]
  end

  subgraph CHAIN["协作链示例 · 漫画工作室"]
    W["编剧 writer"] -->|invoke_npc| SB["分镜 storyboard"]
    SB -->|invoke_npc| AR["画师 artist"]
    AR -->|invoke_npc| LO["排版 layout"]
    LO -->|invoke_npc| VO["配音 voice"]
  end
```

---

## 3. 多世界与多 Agent 社交

```mermaid
flowchart TB
  subgraph WORLDS["4 平行世界"]
    W1[apocalypse 废土]
    W2[comic 漫画]
    W3[default]
    W4[modern 现代]
  end

  subgraph SOCIAL["地图社交 · social + drive"]
    MOVE[drive 移动 / 随机游走]
    CONTACT["social_l2 接触判定<br/>CONTACT_THRESHOLD"]
    INIT["主动发起<br/>base_initiative"]
    TALK["对话 chat_with_tool_loop"]
    MOVE --> CONTACT
    CONTACT --> INIT
    INIT --> TALK
  end

  subgraph COLL["协作链 · invoke_npc"]
    A1["NPC-A"] -->|invoke_npc| A2["NPC-B"]
    A2 -->|产物路径| A3["NPC-C"]
  end

  WORLDS --> SOCIAL
  TALK -.可触发.-> COLL
```

---

## 4. 数据模型（HJL 分形 + 记忆双轨 + 图谱）

```mermaid
flowchart TB
  subgraph FR["HJL 四级分形"]
    WO[("world 世界<br/>物理常数 / 时间")]
    NA[("nation 国家<br/>法律 / 历史")]
    GR[("group 群体<br/>社区共识")]
    IN[("individual 个体<br/>记忆 / 性格")]
    WO --> NA --> GR --> IN
  end

  subgraph NODE["每级 hjl · 三根节点"]
    HDR["header<br/>uuid / parent_id"]
    ATTR["attributes<br/>drive_modifiers / constraints / tools"]
    GPH["graph<br/>nodes / edges"]
  end

  subgraph MEMD["记忆双轨"]
    RAM["RAM buffer<br/>对话期累积 · 内存"]
    PERS["持久化 hjl<br/>对话结束落盘"]
    RAM -->|对话结束| PERS
  end

  subgraph NEO["Neo4j 知识图谱"]
    E[":Entity<br/>kind: person/item/place"]
    E2[":Entity"]
    R[":RELATES<br/>type/observer/confidence"]
    EV[":Event"]
    EV2[":Event"]
    E -->|RELATES| E2
    E -->|PARTICIPATED| EV
    EV -->|NEXT/CAUSED| EV2
  end

  IN -.含三根节点.-> NODE
  IN -.记忆.-> MEMD
  IN -.关系.-> NEO
```

---

## 5. Agentic Loop（一次对话内部）

```mermaid
flowchart TB
  TRIG(["触发<br/>接触 / 玩家 / 定时器 / invoke_npc"])
  TRIG --> CTX["build_context 组装<br/>lore / time / persona / memory / graph / tools"]
  CTX --> GATE{"_should_use_tools?<br/>provider=claude<br/>且有 工具/MCP/task_hint"}
  GATE -->|否| TXT[纯文本 LLM]
  GATE -->|是| TL["工具循环<br/>task_tools + npc_tools + mcp 去重"]
  TL --> LLM(("LLM"))
  LLM -->|工具调用| CALL[执行工具]
  CALL --> INJ{"首次用 skill?"}
  INJ -->|是| SK["activated_skills<br/>按需注入 prompt"]
  INJ -->|否| OBS[观察结果]
  SK --> OBS
  OBS --> WRT[("写记忆 / 图谱")]
  WRT --> LLM
  LLM -->|完成| OUT(["输出"])
  TXT --> OUT
```

---

## 6. 请求全链路

```mermaid
flowchart LR
  EXT["外部入口<br/>前端 / 微信"] --> GW["Gateway<br/>auth + CORS"]
  GW --> DSP["dispatcher 路由<br/>15 模块分支"]
  DSP --> HS["Harness<br/>social_l1"]
  HS --> CH["Channel<br/>llm_l2 选模型"]
  CH --> LLM[("LLM 供应商<br/>zhipu/volcano/local/claude")]
  LLM -.响应原路返回.-> EXT
```

---

## 可继续补

- 工具市场 / MCP 生命周期（marketplace 发现 → 装载 → connect_mcp 懒激活）
- 事件关系处理引擎（写入 → engine 归一 → 观察者置信度 → 多视角并存 → 召回聚合）
- 前端调试面板四 Tab 与后端 API 对应
- 精灵图 / 地图渲染管线（sprite layout → 方向帧 → Y-sort → canvas + overlay）
